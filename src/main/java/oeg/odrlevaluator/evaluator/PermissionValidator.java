package oeg.odrlevaluator.evaluator;

import oeg.odrlevaluator.model.Policy;

/**
 * Determines whether a permission is valid or not.
 * @author victor
 */
public class PermissionValidator {
    
    /**
     * @param p Permission
     * @return true if valid, false if invalid
     */
    public boolean validate(Policy p)
    {
        return false;
    }
    
}
